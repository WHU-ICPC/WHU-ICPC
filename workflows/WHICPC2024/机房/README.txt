机房云桌面部署linux不能增量更新 / 集体关机 / 集体还原，可以手动F2还原
只能将一个配置好的镜像写入所有机器中

ICPC官方镜像可以使用clion，其它ubuntu安装clion会需要license

建议为所有机器开启只允许密钥访问的ssh用于应急 / 一键关机
但今年尝试配置ssh遇到了一些问题没有成功

如果想用安装了ICPC官方镜像的机器运行docker作为评测机的话，需要先apt安装runc